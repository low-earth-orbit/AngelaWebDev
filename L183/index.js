//add html before h1
$("h1").before("<button>New</button>");

// add after selected element
.after()

// before the content of h1 (still inside h1)
.prepend()
//
.append()

// remove button element
$("button").remove();
